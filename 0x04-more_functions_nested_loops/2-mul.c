#include "main.h"

/**
 * mul - check if a character is a digit
 * @x: The number to be multiply
 * @y: The second number to be multiply with y
 * Return: The result of multiplication between x and y para
 */

int mul(int x, int y)
{
	return (x * y);
}
